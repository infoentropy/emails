# Feedback

Refinements requested for features that are already launched — bug reports, tweaks, and follow-up polish, as opposed to net-new work.

If a piece of feedback turns into real implementation work, write a spec for it in `../specs/` (`git mv` the file there, or reference it from a new spec) rather than implementing directly out of this folder.
