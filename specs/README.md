# Specs

Work that's scoped and ready to implement. Each file describes one unit of work — what to build and why — and should be specific enough to start from without further clarification.

When the work described in a spec is done, move the file to `../completed/` with `git mv` (preserves its history) as part of the commit/PR that finishes the work.
